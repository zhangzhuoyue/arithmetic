折半查找又叫二分查找。他的前提是线性表中的记录必须是元素的有序(通常是从小到大)，线性表必须
是顺序存储。折半查找的基本思想：在有序表中，取中间记录作为比较对象，若给定值与中间记录的关键字相等
，则查询成功；若给定值小于中间记录的关键字，则在中间记录的左版区继续查找；若给定值大于中间记录的关键字
，则在中间记录的右半区继续查找。知道查找的结果，或者没有记录。


在 2.两数之和 II - 输入有序数组  3.删除排序数组中的重复项 中的操作的数据结构都是有序的线性表，这两道题的最佳
思考角度都是利用他的有序性，采取双指针法。

2.两数之和 II - 输入有序数组：
从有序表中找到两个元素的和等于给定的数字。他的有序性：从首元素和尾元素的和与给定值
的比较，如果两个元素的和等于给定的元素，则找到了结果；如果两元素之和大于给定值，则尾元素向中间移动；如果两个元素的
和小于给定元素，则首元素向中间移动。一直循环下去，知道找到结果或者查找失败。
该算法和二分查找的思想是一样，在元素有序的情况下，找到参考数据，判断两个指针元素的移动方向，

3.删除排序数组中的重复项：
首先考虑到他是有序的线性存储结构。利用他的有序性可以使用双指针。首先，要